version: '3.8'
services:
    webserver:
        image: sciohub/laravel-nginx
        container_name: nginx-laravel
        restart: unless-stopped
        ports:
            - 50100:80

    php:
        image: sciohub/innovation-catalogue-connect:php-dev
        container_name: laravel-app
        restart: unless-stopped